---
- name : allocate swap file
  command:
    cmd: "fallocate -l {{swap_size}} /home/ec2-user/{{swap_size}}.swap"

- name : display file information
  command:
    cmd: "stat /home/ec2-user/{{swap_size}}.swap"
