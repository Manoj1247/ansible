---
- name : enable swap
  command:
    cmd: "swapon /home/ec2-user/{{swap_size}}.swap"

- name : copy the file
  command:
    cmd: echo '/home/ec2-user/.swap swap swap defaults 0 0' | sudo tee -a /etc/fstab